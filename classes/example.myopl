CLASS Person
	FUN Person(name)
		VAR this.name = name # can also use "VAR self.name = name"
	END
	FUN get_name()
		RETURN this.name
	END
END

VAR p1 = Person("John Stone")
PRINT(p1.get_name())
VAR p2 = Person("James Johnson")
PRINT(p2.get_name())

EXIT()
