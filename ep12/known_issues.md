Any expression that doesn't return anything (that returns None in Python) may result in a Python runtime error if used in other expressions.

Examples:

 - `VAR test = IF 0 == 1 THEN "Python will raise a runtime error"`
